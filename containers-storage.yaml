project-templates:
  containers/storage:
    branches:
    - master
    gitweb: https://github.com/containers/%(name)s/commit/%%(sha)s
    tags:
    - Development
    uri: https://github.com/containers/%(name)s
projects:
  containers/storage:
    description: A library for storing and mounting container images
    repos:
      storage:
        branches:
        - master
        template: containers/storage
