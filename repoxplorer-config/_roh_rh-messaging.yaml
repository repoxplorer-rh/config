project-templates:
  rh-messaging:
    branches:
    - master
    gitweb: https://github.com/rh-messaging/%(name)s/commit/%%(sha)s
    uri: https://github.com/rh-messaging/%(name)s
projects:
  rh-messaging:
    description: Messaging tools for developers
    repos:
      Artemis-JON-plugin:
        branches:
        - master
        template: rh-messaging
      a-mq-migration:
        branches:
        - master
        template: rh-messaging
      amq-streams-docs:
        branches:
        - master
        template: rh-messaging
      amq7-training:
        branches:
        - master
        template: rh-messaging
      amqonline-workshop:
        branches:
        - master
        template: rh-messaging
      artemis-hawtio:
        branches:
        - 1.0.x
        template: rh-messaging
      artemis-wildfly-integration:
        branches:
        - master
        template: rh-messaging
      cli-cpp:
        branches:
        - master
        template: rh-messaging
      cli-java:
        branches:
        - master
        template: rh-messaging
      cli-netlite:
        branches:
        - master
        template: rh-messaging
      cli-proton-go:
        branches:
        - master
        template: rh-messaging
      cli-proton-python:
        branches:
        - master
        template: rh-messaging
      cli-proton-ruby:
        branches:
        - master
        template: rh-messaging
      cli-rhea:
        branches:
        - master
        template: rh-messaging
      jira-git-report:
        branches:
        - master
        template: rh-messaging
      messaging-versioning:
        branches:
        - master
        template: rh-messaging
      rh-messaging-pom:
        branches:
        - master
        template: rh-messaging
