project-templates:
  shim:
    branches:
    - master
    gitweb: https://github.com/rhboot/%(name)s/commit/%%(sha)s
    uri: https://github.com/rhboot/%(name)s
projects:
  shim:
    description: The UEFI first stage bootloader for Secure Boot
    repos:
      shim:
        branches:
        - master
        template: shim
