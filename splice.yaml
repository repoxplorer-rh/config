project-templates:
  splice:
    branches:
    - master
    gitweb: https://github.com/splice/%(name)s/commit/%%(sha)s
    tags:
    - Operations
    uri: https://github.com/splice/%(name)s
projects:
  splice:
    description: Reporting tool
    repos:
      gofer:
        branches:
        - master
        template: splice
      python-certutils:
        branches:
        - master
        template: splice
      report_server:
        branches:
        - master
        template: splice
      rhic-serve:
        branches:
        - master
        template: splice
      spacewalk-splice-tool:
        branches:
        - master
        template: splice
      splice-certmaker:
        branches:
        - master
        template: splice
      splice-init:
        branches:
        - master
        template: splice
      splice-reports:
        branches:
        - master
        template: splice
      splice-server:
        branches:
        - master
        template: splice
