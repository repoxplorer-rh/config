project-templates:
  wrapanapi:
    branches:
    - master
    gitweb: https://github.com/ManageIQ/%(name)s/commit/%%(sha)s
    uri: https://github.com/ManageIQ/%(name)s
projects:
  wrapanapi:
    description: Python-based library for provider communication abstraction
    repos:
      wrapanapi:
        branches:
        - master
        template: wrapanapi
